{
  "version": 2,
  "builds": [
    {
      "src": "api/index.ts", 
      "use": "@vercel/node",
      "config": {
        "installCommand": "npm install --save-dev prisma && npm install && npx prisma generate",
        "buildCommand": "npx prisma generate && pnpm run build"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "api/index.ts" 
    }
  ],"scripts" {
      "postinstall": "npx prisma generate"
  }
}
